# API响应字段修正总结

## 已完成修正

根据实际联调结果，`docs/修正总结.md` 旧版关于响应字段的理解有误。当前代码和文档已统一为平台实际成功判断：

```python
result.get("result") == "success" or result.get("result_val") == 0
```

## 核心修正内容

### 响应字段的正确定义

```python
# API成功响应示例
{
    "result": "success",  # 字符串，success 表示成功
    "result_val": 0,      # 数字，0 表示成功
    # ... 其他业务数据
}
```

### 关键要点

| 字段 | 类型 | 成功值 | 失败值 | 说明 |
|------|------|--------|--------|------|
| `result` | 字符串 | `"success"` | 错误描述文本 | 处理结果说明 |
| `result_val` | 数字 | `0` | `1`, `2`, `3`... | 处理结果码 |

### 正确的判断方式

```python
# 正确
if result.get("result") == "success" or result.get("result_val") == 0:
    print("成功")

# 错误
if result.get("result") == "0":
    print("成功")
```

## 已同步的文件

- `api_client.py`
  - `is_success_response()` 已恢复为 `result == "success" or result_val == 0`
  - 错误日志格式使用 `result_val` 作为错误码，`result` 作为错误说明

- `main.py`
  - 通过 `is_success_response()` 统一判断 API 成功

- `quick_test.py`
  - 登录和版本接口测试使用统一成功判断
  - 登录失败时按 `result_val` 错误码映射提示

- `test_api.py`
  - 通过 `is_success_response()` 统一断言 API 成功

- `matrix_service.py`
  - 设备列表、大屏列表、开窗结果都使用统一成功判断
  - 开窗前会读取 `GetDisplayWallWnds` 并关闭覆盖目标输出区域的旧窗口
  - 开窗后会再次读取 `GetDisplayWallWnds`，确认 `OpenWnd` 返回的 `handle`
    或目标坐标里存在期望信号源，避免只看 success 却不知道物理屏为何无画面
  - 如果目标输出区域已经存在同一输入源的正确窗口，会直接复用该窗口，避免重复
    点击同一路矩阵命令时先关窗再重开触发底层 SDK `-10`
  - Web 页面取流通道会识别平台返回的 8 段编码器 MAC，例如
    `6c-df-fb-01-5e-80-00-01`，避免重复拼成
    `6c-df-fb-01-5e-80-00-01-00-01/v3`
  - WebSocket 取流地址会带上当前大屏名，例如
    `ws://192.168.130.101:8003/?display_wall=VW3`，避免只连接 8003 根地址导致
    TCP 可达但 WebSocket 握手超时
  - 会读取 `GetEncoderInfo` 的 `video_stream` 字段并输出输入流编码，便于判断
    Web 页面黑屏是取流失败、H.265 不支持，还是 H.264 浏览器解码失败
  - 返回 `stream.candidates` 给前端，包含当前配置版本以及 `/v1`、`/v2`、
    `/v3` 候选通道，用于 Web 预览自动重试

- `web_server.py`
  - 本地矩阵接口成功响应统一为 `{"result": "success", "result_val": 0}`
  - 增加 `/api/preview/event` 诊断接口，记录浏览器预览取流事件
  - 当浏览器 0 帧阶段连接失败时，会快速检查 `ws_url` 的 TCP 端口是否可达
  - TCP 可达时继续做 WebSocket Upgrade 握手检查，区分端口可达但握手无响应、
    服务端拒绝握手等情况
  - 握手检查会分别尝试带 `Origin` 和不带 `Origin`，用于区分跨域限制和
    取流服务未按标准 WebSocket 响应当前地址

- `web/static/app.js`
  - 前端成功判断同步为 `result === "success" || result_val === 0`
  - 预览窗口会显示 `未收到码流`、`收到 H.265`、`H.264 解码失败` 等状态
  - 如果当前通道无码流、返回 H.265 或浏览器解码失败，会自动尝试下一候选通道
  - 会回传 `candidate_start`、`first_frame`、`candidate_failed`、`decode_ok`
    等事件到后端日志，方便只看 Python 控制台也能定位黑屏原因
  - WebSocket 连接 5 秒未打开时判定为 `连接超时`，避免长时间等待浏览器默认超时

- `docs/分布式综合运维管理平台API学习笔记.md`
  - 登录响应字段说明和全局注意事项已同步

- `CHANGELOG.md`
  - 响应字段修正说明已同步

## 使用示例

```python
from api_client import APIClient, is_success_response, format_api_error

client = APIClient()
success = client.login("admin", "123456")

if success:
    result = client.get_device_overview()
    if is_success_response(result):
        print("获取成功:", result.get("result"))
    else:
        print(f"获取失败 {format_api_error(result)}")
```

## 注意事项

1. 成功判断应优先复用 `api_client.is_success_response()`。
2. `result_val` 是错误码字段，成功时为数字 `0`。
3. `result` 是结果说明字段，成功时为字符串 `"success"`。
4. 不要再使用 `result == "0"` 作为成功判断。
5. `OpenWnd` 成功只表示平台接受开窗命令；仍需用 `GetDisplayWallWnds`
   验证窗口是否真实存在于目标输出区域。如果日志已有 `开窗确认` 但物理屏无画面，
   应继续检查编码器输入信号、设备在线状态、解码器 HDMI 输出和显示器输入源。
6. Web 页面预览是独立的 WebSocket 取流链路。如果平台返回的编码器 MAC
   已包含 `00-01` 通道号，取流通道应为 `MAC/v3`，不能再重复追加
   `-00-01/v3`。
7. 物理大屏窗口存在且输入状态正常时，Web 预览仍可能因为 8003 未回帧、
   浏览器不支持 H.265 或 H.264 AnnexB 解码兼容性导致黑屏，应结合页面状态
   和 `video_stream` 日志继续定位。
8. 编码器可能提供多个取流版本。默认取流版本不一定是浏览器可解的 H.264，
   因此前端需要按候选通道自动重试，而不是只固定使用 `/v3`。
9. 由于 WebSocket 取流由浏览器直连 8003，原本 Python 后端看不到取流过程；
   需要前端主动回传诊断事件，才能从后端日志判断是否收帧和是否解码成功。
10. 若预览日志显示 `连接超时` 或 `取流异常` 且端口检查为 `tcp_failed`，
    应优先处理取流服务 8003 可达性，而不是继续调整大屏开窗参数。
11. 如果 TCP 可达但握手检查为 `handshake_timeout` 或 `handshake_rejected`，
    说明黑屏原因在 WebSocket 握手阶段，需要检查 8003 取流服务协议、Origin
    限制或连接路径。如果带 `Origin` 和不带 `Origin` 都是 `handshake_timeout`，
    则优先怀疑当前 `ws://服务器:8003` 地址不是平台实际的标准 WebSocket 入口。
12. 实测 8003 取流需要在连接地址上携带当前大屏名，例如
    `ws://192.168.130.101:8003/?display_wall=VW3`；缺少 `display_wall` 时会出现
    端口 `tcp_ok` 但握手 `handshake_timeout`。

---

**修正完成时间**: 2026-08-22  
**修正依据**: 实际联调返回格式  
**修正状态**: 已完成并验证
